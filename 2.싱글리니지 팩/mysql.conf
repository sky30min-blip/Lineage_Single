################################################
#
#	MariaDB ���� ���� (MySQL �������� ȣȯ)
#
################################################
driver = org.mariadb.jdbc.Driver
# MariaDB 전용 설정 파일(파일명은 mysql.conf 유지). 드라이버: org.mariadb.jdbc.Driver
# 이 PC 기준: 3306·3307 Windows 서비스는 root/1307 거부·게임 DB 없음(test만). 게임·GM 연결은 Docker MariaDB 호스트 3308 + l1jdb.
# 윈도우 MariaDB에 lin200 을 만들고 root 비번을 1307 로 맞춘 뒤에는 아래 포트를 3306 또는 3307 로 바꾸면 됨.
url = jdbc:mariadb://localhost:3308/l1jdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&sslMode=DISABLE
id = root
pw = 1307

# ���ӱ� ��ü���� �ڵ��Ŀ� ����� ������ ��츸 ��밡��.
# �ڵ��Ŀ� ��� ����(true: ��� / false: ������)
is_donation = false
# �ڵ��Ŀ� ����
donation_url = jdbc:mariadb://localhost:3308/donation?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&sslMode=DISABLE

################################################
# �ڵ���� ������� ����
auto_backup = false
################################################
# �ڵ���� �ֱ� ����
#	������ ������ �ð��� �������� �����̰� ����.
#	�д���.
auto_backup_delay = 1

################################################
# ��� ��� ����.
#auto_backup_path = C:/backup
auto_backup_path = server_backup
